/**
 * IPL / app.js — LEGADO DESATIVADO
 *
 * Este arquivo não é carregado por `ipl/index.html`. A implementação ativa do
 * módulo IPL está atualmente encapsulada no próprio `index.html`, incluindo
 * fontes, metodologia de cálculo, filtros, SARC e camada territorial.
 *
 * Mantido apenas para preservar a estrutura histórica do módulo e evitar
 * quebra de referências antigas. Não contém configuração de produção.
 */
