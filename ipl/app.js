/**
 * HUB COMLURB · IPL
 * ARQUIVO LEGADO DESATIVADO
 *
 * A implementação ativa do módulo está autocontida em `ipl/index.html`.
 * Este arquivo é mantido apenas para compatibilidade histórica do diretório.
 * Não contém fontes, regras ou inicialização de produção e não é carregado pela página.
 */
window.HUB_IPL_LEGACY_DISABLED = true;
