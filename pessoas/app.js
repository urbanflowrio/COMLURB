/**
 * pessoas/app.js — DESATIVADO
 *
 * Este arquivo continha a inicialização do módulo, os filtros interativos
 * e a função exportExcel(), que gerava um arquivo .xlsx com Registro, Nome,
 * Idade, Sexo, Cidade, Afastamento, TiposLaudos e QtdLaudos por servidor.
 *
 * Removido nesta versão por risco crítico de LGPD. Este arquivo não contém
 * mais nenhuma lógica de exportação nem dado individual. index.html não o
 * inclui mais; a página atual é renderizada por um script inline mínimo,
 * limitado a header e footer institucionais.
 */
