/**
 * pessoas/data.js — DESATIVADO
 *
 * Este arquivo carregava anteriormente bases individualizadas de servidores
 * (planilhas R54, LAUDOS e ORGANOGRAMA), incluindo nome, matrícula, idade,
 * bairro de residência, afastamento e tipos de laudo médico, diretamente
 * no navegador.
 *
 * Isso foi identificado como risco crítico de LGPD (exposição de dado
 * pessoal e de saúde individualizado, incluindo busca nominal e exportação
 * em massa) e foi removido nesta versão.
 *
 * Este arquivo não contém mais nenhuma URL de planilha, nenhuma lógica de
 * carregamento e nenhum dado pessoal. Ele é mantido apenas para não quebrar
 * referências antigas em cache do navegador; index.html não o inclui mais.
 *
 * O módulo só deve voltar a consumir dados quando existir uma fonte já
 * agregada e anonimizada, com piso mínimo por grupo, aprovada para
 * publicação.
 */
